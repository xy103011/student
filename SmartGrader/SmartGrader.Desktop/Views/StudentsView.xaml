<UserControl x:Class="SmartGrader.Desktop.Views.StudentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- 工具栏 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="加载学生" 
                    Style="{StaticResource ActionButtonStyle}"/>
            <Button Content="添加学生" 
                    Style="{StaticResource ActionButtonStyle}"/>
            <Button Content="导入学生" 
                    Style="{StaticResource ActionButtonStyle}"/>
        </StackPanel>
        
        <!-- 学生列表 -->
        <DataGrid Grid.Row="1" 
                  AutoGenerateColumns="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="80"/>
                <DataGridTextColumn Header="姓名" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="学号" Binding="{Binding StudentId}" Width="150"/>
                <DataGridTextColumn Header="班级ID" Binding="{Binding ClassId}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
